import { FileMessage } from '@sendbird/chat/message';

interface Props {
    message: FileMessage;
    onClose: () => void;
}
export declare const FileViewer: ({ message, onClose }: Props) => import("react/jsx-runtime").JSX.Element;
export {};
