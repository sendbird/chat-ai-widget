import { FileMessage as ChatFileMessage } from '@sendbird/chat/message';

type Props = {
    message: ChatFileMessage;
};
export default function FileMessage(props: Props): import("react/jsx-runtime").JSX.Element;
export {};
