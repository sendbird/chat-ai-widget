import { SBUFoundationProps } from '../../types';

type Props = SBUFoundationProps<{
    size?: string | number;
}>;
export declare const Loader: ({ className, children, size, testId }: Props) => import("react/jsx-runtime").JSX.Element;
export {};
